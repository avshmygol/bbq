<%= form_for [@event, subscription], data: { turbo: false } do |f| %>
    <%= render partial: 'partials/form_errors', locals: {model: subscription} %>

    <% unless user_signed_in? %>
      <div class="mb-3">
        <%= f.label :user_name %>
        <%= f.text_field :user_name, class: 'form-control' %>
      </div>

      <div class="mb-3">
        <%= f.label :user_email %>
        <%= f.text_field :user_email, class: 'form-control' %>
      </div>
    <% end %>

    <div class="mb-3">
      <%= f.button class: 'btn', style: 'background-color: #8199A3; color: white;' do %>
        <i class='bi bi-check-lg'></i>&nbsp;&nbsp;Записаться
      <% end %>
    </div>
<% end %>