<h1>Доступ только по пин-коду</h1>

<%= form_tag('', method: 'post', data: { turbo: false }) do |f| %>
  <div class="mb-3">
    <%= label :pincode, 'Пин-код' %>
    <%= text_field_tag :pincode, params[:pincode], class: 'form-control'%>
  </div>

  <div class="mb-3">
    <%= button_to class: 'btn', style: 'background-color: #8199A3; color: white;' do %>
      <i class='bi bi-check-lg'></i> Ввести
    <% end %>
    или <%= link_to 'вернуться', :back %>
  </div>
<% end %>
